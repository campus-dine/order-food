# 1. ???????
server:
  port: 8080
  servlet:
    context-path: /api # ??????????http://localhost:8080/api/doc.html

# 2. Spring ?????MySQL?Redis??????
spring:
  profiles:
    active: dev # ????????

  # 2.1 ??????? (MySQL 8.0)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ? ????? 192.168.138.132 ????????? IP?order_food_db ????????
    url: jdbc:mysql://192.168.138.132:3306/order_food_db?useUnicode=true&characterEncoding=utf-8&userSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    # ? ?????? MySQL ???????
    username: root
    password: root
    hikari:
      minimum-idle: 5
      maximum-pool-size: 15
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000

  # 2.2 ???? (Redis)
  redis:
    # ? ????????? IP
    host: 192.168.138.132
    port: 6379
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

  # 2.3 ??????????????????????? @Value ???
  wx:
    miniapp:
      appid: wx6a720cf1987ad9bf     # ? ????????? AppID
      secret: a6766c03f8845c940ddc9cab975455c6     # ? ????????? AppSecret

# 3. MyBatis-Plus ????
mybatis-plus:
  # ?? MyBatis-Plus ?????? Mapper.xml ??????????XML??????
  mapper-locations: classpath:mapper/**/*.xml
  # ?????????????
  type-aliases-package: com.weapp.order_food.entity
  configuration:
    # ????????????????? order_id ??? Java ?? orderId?
    map-underscore-to-camel-case: true
    # ? ???????????????? SQL ???????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 4. Knife4j (Swagger3) ??????
knife4j:
  enable: true
  setting:
    language: zh-CN # ????????
    enable-swagger-models: true
    enable-reload-cache: true
    enable-search: true
  openapi:
    title: "???????-??????"
    description: "?? Spring Boot 2.7.15 + MyBatis-Plus + Redis ?????"
    version: v1.0
    concat: "??????"
    url: "https://localhost:8080/api/doc.html"
    group:
      default:
        group-name: "??????"
        api-rule: package
        api-rule-resources:
          - com.weapp.order_food.controller # ? ???????? Controller ???????

# 5. ??? JWT (Json Web Token) ?????????? JJWT 0.12.6?
jwt:
  # ? ??????????? JJWT?????????????? 256 ??32???/???????????
  secret: secure_and_long_secret_key_for_order_food_app_2026
  # ??????????????? 7 ??7 * 24 * 60 * 60 * 1000?
  expiration: 604800000
  # ?????? Token ???
  header: Authorization